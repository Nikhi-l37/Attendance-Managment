{
  "name": "student-attendance-and-marks-management-system",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:render": "npm run build && cd server && npm ci --omit=dev && cd ..",
    "preview": "vite preview",
    "server": "node server/server.js",
    "seed": "node server/seed.js",
    "start": "node server/server.js"
  },
  "dependencies": {
    "react": "^19.2.0",
    "recharts": "^3.3.0",
    "react-dom": "^19.2.0",
    "serve": "^14.2.4"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
